generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./lang.sqlite"
}

model hello {
  id       Int     @id @default(autoincrement())
  text     String?
  language String?
}
